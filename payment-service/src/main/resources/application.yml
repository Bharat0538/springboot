server:
  port: 9191

spring:
  h2:
    console:
      enabled: true
      path: /h2-console
  datasource:
    url: jdbc:h2:mem:payment
    username: sa
    password:
    driverClassName: org.h2.Driver

  application:
    name: PAYMENT-SERVICE
  cloud:
    config:
      uri:
        - http://localhost:9691